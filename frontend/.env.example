# Leave this blank for local development — Vite's dev server proxy
# (see vite.config.js) forwards /api requests to your local backend.
#
# For production (Vercel), set this to your deployed backend's URL, e.g.:
# VITE_API_URL=https://voicetask-backend.onrender.com
VITE_API_URL=
